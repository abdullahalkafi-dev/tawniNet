import 'package:awnneaapp/app/modules/common/views/app_notification_list_view.dart';
import 'package:flutter/material.dart';

/// Helper notification center — real history from API.
class HelperNotificationView extends StatelessWidget {
  const HelperNotificationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppNotificationListView();
  }
}
